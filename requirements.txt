# Core dependencies
torch>=1.9.0
opencv-python>=4.5.0
pyyaml>=5.4.0
requests>=2.25.0
psutil>=5.8.0
paho-mqtt>=1.5.0
click>=8.0.0
ultralytics>=8.0.0

# ROS2 dependencies (install via apt/rosdep or colcon)
# These packages are typically installed as part of ROS 2 setup:
# - rclpy
# - sensor-msgs
# - std-msgs  
# - cv-bridge
# - diagnostic-updater
# - diagnostic-msgs
# - camera-info-manager-py (ros2 package)
#
# To install ROS 2 dependencies:
# 1. Source your ROS 2 installation: source /opt/ros/<distro>/setup.bash
# 2. Install via apt: sudo apt install ros-<distro>-camera-info-manager-py
# 3. Or build from source with colcon if needed

# Development dependencies
pytest>=6.0.0
flake8>=3.9.0
mypy>=0.812
sphinx>=4.0.0
black>=21.0.0
isort>=5.9.0

# Testing dependencies
pytest-mock>=3.6.0
pytest-cov>=2.12.0